#ifndef BUFFER_CONFIG_HPP
#define BUFFER_CONFIG_HPP

#include <stddef.h>

// Buffer类使用
constexpr size_t MIN_PREPENDABLE_SIZE = 8;

constexpr size_t DEFAULT_BUFFER_SIZE = 1024;


// FixedBuffer类使用
constexpr size_t MAX_BUFFER_SIZE = 65535;

#endif // BUFFER_CONFIG_HPP